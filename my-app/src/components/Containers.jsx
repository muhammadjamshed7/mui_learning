import { Container, Typography } from "@mui/material";
const Containers = () => {
	return (
		<>
			<Container sx={{ background: "lightgreen", marginTop: "40px" }}>
				<Typography variant="body2">This is a container for xs</Typography>
			</Container>
		</>
	);
};

export default Containers;
